{
    "title": "Input Schema",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "query": {
            "title": "Building Task",
            "type": "string",
            "description": "The specific task or question about building codes (e.g. 'Find single house building requirements in Antioch, CA').",
            "editor": "textarea",
            "example": "Find single house building requirements in Antioch, CA."
        },
        "modelName": {
            "title": "Model Name",
            "type": "string",
            "description": "LLM model to use (default: gpt-4o-mini).",
            "default": "gpt-4o-mini",
            "editor": "textfield"
        },
        "debug": {
            "title": "Debug Mode",
            "type": "boolean",
            "description": "Enable debug logging.",
            "default": false
        }
    },
    "required": [
        "query"
    ]
}